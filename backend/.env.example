# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

# Para Supabase: use a URL de conexão direta (porta 5432) com app_user quando RLS estiver ativo.
# Para RLS funcionar corretamente, use connection_limit=1 para que set_config('app.current_user_id') persista na mesma conexão do request.
# Ex.: postgresql://app_user:SENHA@db.xxx.supabase.co:5432/postgres?connection_limit=1
DATABASE_URL="[YOUR_DATABASE_URL_HERE]"
JWT_SECRET="[YOUR_JWT_SECRET_HERE]"
JWT_EXPIRATION="1d" # Exemplo: 1 dia

# Configurações de E-mail (SMTP)
EMAIL_HOST="smtp.seudominio.com" # Ex: smtp.gmail.com, smtp.sendgrid.net
EMAIL_PORT="587" # Geralmente 587 para TLS, 465 para SSL
EMAIL_USER="seu_email@seudominio.com"
EMAIL_PASS="sua_senha_do_email" # Ou chave de API para serviços como SendGrid
EMAIL_FROM="Seu App de Finanças <noreply@seudominio.com>" # Remetente padrão
FRONTEND_URL="http://localhost:5173" # URL do seu frontend (para o link de reset)

# Configurações do Supabase storage
SUPABASE_ENDPOINT = "supabase storage endpoint"
SUPABASE_REGION = "supabase storage region"
SUPABASE_ACCESS_KEY_ID = "supabase storage access key id"
SUPABASE_SECRET_ACCESS_KEY = "supabase storage secret access key"
SUPABASE_SERVICE_ROLE_KEY = "supabase service role key"
SUPABASE_URL = "supabase project url"

# Stripe (webhooks de assinatura)
STRIPE_WEBHOOK_SECRET="whsec_..." # Signing secret do endpoint em Developers > Webhooks
STRIPE_SECRET_KEY="sk_test_..." # Opcional; usado para inicializar o SDK (webhook só precisa do secret)